return {
  notification = {
    window = {
      winblend = 0,
      max_width = 80,
      max_height = 40,
      border = "rounded",
    },
  },
  progress = {
    suppress_on_insert = true,
    ignore_empty_message = true,
    ignore = {}, -- List of LSP servers to ignore
  },
}
